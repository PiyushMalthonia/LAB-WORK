#include<stdio.h>
void main()
{
    int age;
    printf("enter the age");
    scanf("%d",&age);
    if(age>=19 && age<=50)
    if(age>=18 && age<=25)
    {
        printf("eligible for grade a job");
    }
    else
    {
        printf("eligible for grade b job");
    }
    else
    {
    printf("young or not eligible for job");
    }
}