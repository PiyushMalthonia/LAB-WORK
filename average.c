#include<stdio.h>
int avg(int[],int);
void main()
{
    int mark[]={77,88,99,88,99};
    int size;
    size=sizeof(mark)/sizeof(mark[0]);
    average=avg(mark,size);
    printf("average=%d\n",average);
}
int avg(int mark[1],int size)
{
int i,sum
}